# Список завдань (task.md)

- [x] 1. Додавання режиму `raw=True` у `build_speaker_pool()` (`core/speaker_resolution.py`) без перекладу глосарієм для вихідної ідентичності
- [x] 2. Підтримка `speaker_pool` у `GlossaryManager.bind_project_rows()` та `_append_owned_occurrences()` (`core/glossary_manager.py`)
- [x] 3. Передача `raw_pool` у `bind_project_rows()` під час відкриття/оновлення діалогу глосарію (`handlers/translation/glossary_handler.py`)
- [x] 4. Передача `raw_pool` у `bind_project_rows()` на старті пайплайну глосарію (`handlers/translation/glossary_pipeline_handler.py`)
- [x] 5. Модульні тести для raw пулу, входжень та пайплайну (`test_speaker_pool.py`, `test_glossary_occurrence_bridge.py`, `test_glossary_pipeline_coordinator.py`, `test_glossary_pipeline_handler.py`)
- [x] 6. Ітерація версії до `0.3.088-dev`, перевірка `ruff`, `git diff --check` та оновлення документації
- [x] 7. Повний прогін тестового комплексу (`test_all.ps1`) та виправлення зауважень Ruff, інваріантів плагінів і тестів UI
- [x] 8. Формування консолідованого ченджлогу релізу `v0.3.090` від базового релізу `v0.3.068`
- [x] 9. Оновлення документації (`CHANGELOG.md`, `README.md`, `GEMINI.md`, `utils/constants.py`)
- [x] 10. Створення тегу `v0.3.090`, публікація релізу на GitHub без бінарників та бамп версії для наступного циклу розробки
- [x] 11. Усунення затримки під час застосування перекладу входження глосарію: прибрано повну синхронну перебудову блоку та дубльоване оновлення дерева; додано регресійні тести для звичайного й віртуального подань; версія `0.3.097-dev`.
- [x] 12. Профілювання фактичного шляху підтвердження варіанта глосарію та усунення повного повторного сканування проєкту при незмінних терміні й категорії; версія `0.3.098-dev`.
- [x] 13. Реорганізація діалогу глосарію (`GlossaryDialog`): відображення оригіналу поруч із перекладом, інтеграція посилання на Zelda Wiki (`BaseGameRules.get_external_reference_url`), згортання секцій опису/нотаток/входжень, компактна кнопка підтвердження, переміщення чекбокса "Profiled via AI" та роздільні фільтри входжень (Mentions / Spoken); версія `0.3.099-dev`.
- [x] 14. Вирівнювання вертикального рівня полів оригіналу та перекладу (`QGridLayout`, `setFixedHeight(26)`) і зменшення виділеного місця під оригінал (пропорція 1:2, `maximumWidth(280)`); версія `0.3.100-dev`.
- [x] 15. Перенесення чекбокса «Потребує перевірки» («Needs review») з рядка пошуку в нижню панель під таблицю термінів і перекладів (`_tab_widget`); версія `0.3.101-dev`.
- [x] 16. Створення скіла релізу та деплою `deploy` (`.agents/skills/deploy/SKILL.md` та `.grok/skills/deploy/SKILL.md`), додавання пакетного режиму (`--batch-size`) у конвеєр перекладу інтерфейсу `tools/i18n-translate/translate.py`, відновлення збереження зірочок підрядків при навігації (`handlers/list_selection/physical_selection_mixin.py`, `test_asterisk_logic.py`) та узгодження тестів асинхронного сканування (`test_tag_validation.py`); реліз `v0.3.101`.
- [x] 17. Публікація релізу `v0.3.101` на GitHub без бінарників та бамп версії для наступного циклу розробки `0.3.102-dev`.
- [x] 18. Оновлення та розширення скіла `update-wiki` (`.agents/skills/update-wiki/SKILL.md` та `.grok/skills/update-wiki/SKILL.md`) для автоматичного оновлення вікі (`docs/wiki/`) та `README.md` при додаванні нових функцій або перенесенні/зміні елементів меню та інтерфейсу; синхронізація поточної документації та вікі.
